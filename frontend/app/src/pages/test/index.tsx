import { For, Show, createEffect, onMount } from "solid-js";
export function TestPage() {
  return (
    <main class="flex flex-row justify-center">
      <section class="w-256 flex flex-col gap-4"></section>
    </main>
  );
}
